# 虚拟机验证报告

在 VMware 虚拟机上对本系统做了一次端到端验证：完整跑通教师机—学生机的部署、
四种网络状态切换与卸载流程，并对若干缺陷做了修复与复验。

本文记录验证方法、发现的问题及其成因分析，供后续维护和部署参考。

> 文中所有 IP 均为实验环境的私有地址，与任何真实部署环境无关。

---

## 目录

- [一、结论](#一结论)
- [二、测试环境](#二测试环境)
- [三、验证方法](#三验证方法)
- [四、功能验证：九项通过](#四功能验证九项通过)
- [五、发现并修复的缺陷](#五发现并修复的缺陷)
- [六、已确认但未修复的问题](#六已确认但未修复的问题)
- [七、部署与测试经验](#七部署与测试经验)
- [附录：名词解释](#附录名词解释)

---

## 一、结论

系统的核心功能是可靠的：四种网络状态切换、规则下发、状态持久化、重连恢复
均按设计工作。

但**开机自动断网（fail-closed）这条路径存在两个严重缺陷**，会导致学生机开机后
无法连回教师机，从而永久停留在断网状态且无法远程恢复。这两个缺陷只有在真实
部署环境下才会暴露，仅靠阅读代码无法发现。

一共修复七个问题，其中三个是本次真机验证才发现的。

| 编号 | 问题 | 发现方式 | 严重程度 |
|------|------|---------|---------|
| 一 | 同网段教师机被"保命路由"挡在门外 | 真机 | 严重 |
| 二 | 教师机上报了错误的自身 IP | 真机 | 严重 |
| 三 | 卸载竞态导致网络恢复被中断 | 代码审查 | 严重 |
| 四 | 路由清理超命令行长度上限 | 代码审查 | 严重 |
| 五 | HTTP 控制接口可被 CSRF | 代码审查 | 严重 |
| 六 | 依赖清单缺两个包 | 代码审查 | 中 |
| 七 | 卸载脚本与自动重启策略冲突 | 真机 | 中 |

---

## 二、测试环境

模拟真实机房：宿主机扮演教师机，虚拟机扮演学生机。

```
   ┌─────────────────────────────┐
   │  宿主机（教师机）              │
   │                             │
   │  主控端.exe                  │
   │    WebSocket  :8765         │
   │    HTTP 接口  :8770         │
   │                             │
   │  网卡一：172.16.5.20         │  ← 通往互联网的网卡
   │  网卡二：192.168.31.1        │  ← 虚拟机走这张
   └──────────┬──────────────────┘
              │  NAT（网段 192.168.31.0/24）
   ┌──────────┴──────────────────┐
   │  虚拟机（学生机）              │
   │                             │
   │  CTR.exe（Windows 服务）      │
   │    以 SYSTEM 身份运行         │
   │                             │
   │  IP：192.168.31.132          │
   │  网关：192.168.31.2          │
   └─────────────────────────────┘
```

**这个拓扑有一个关键特征：教师机是双网卡的。**
问题二就是被这个特征暴露出来的。真实机房里的教师机（有线 + 无线，
或装有 VPN、虚拟机软件）同样是多网卡，因此这不是虚拟机特有的情况。

| 项目 | 内容 |
|------|------|
| 虚拟化软件 | VMware Workstation |
| 客户机系统 | Windows 10 专业版 19045（22H2） |
| 客户机配置 | 12 vCPU，8 GB 内存 |
| 网络模式 | NAT |
| 测试白名单 | 豆包（含字节系 CDN 域名） |
| 测试黑名单 | DeepSeek |
| 对照组 | 百度（既不在白名单也不在黑名单） |

白名单里除主域外还需补齐 CDN 域名。现在的网站首页只是个壳，图片、脚本、
接口都在 CDN 域名上；只放行主域会导致页面打开但内容空白，那属于白名单配置
不全，不是系统故障。

---

## 三、验证方法

### 3.1 核心难题

测试这套系统有个天然困难：**被测系统会主动切断网络**。一旦学生机进入断网或
白名单状态，远程桌面、SSH、共享文件夹等常规手段全部失效。

### 3.2 解决办法

使用 VMware 的 `vmrun` 工具。它与虚拟机的通信不走网络，而是走 VMware 自身的
虚拟机管理通道，因此即使虚拟机完全断网，仍可传文件、执行命令、截屏、
以及断开虚拟网卡模拟拔网线。

### 3.3 每个测试节点采集的数据

| 采集项 | 意义 |
|--------|------|
| 默认路由是否存在 | **最核心的判据**。系统靠删除默认路由实现断网 |
| `/32` 主机路由条数 | 白名单每放行一个 IP 加一条，可反映动态放行是否工作 |
| 网卡 DNS 指向 | 过滤模式下应为 `127.0.0.1` |
| 本地 53 端口监听状态 | 本地 DNS 过滤服务是否启动 |
| 服务状态与启动类型 | 服务存活与开机自启配置 |
| 防火墙策略与 `NC_` 规则数 | 检查是否有残留的防火墙改动 |
| 到教师机的 TCP 连通性 | 断网状态下能否连回教师机 |
| 三个网站的 DNS 解析结果 | 白/黑名单是否生效 |
| 三个网站的 HTTP 访问结果 | **最终判据** |

最后一项尤为重要：**DNS 解析成功不等于能上网**——可能解析成功但路由不通。
必须实际发起 HTTP 请求验证。

### 3.4 状态切换的触发方式

"允许上网"与"禁止上网"可通过 HTTP 接口触发；"启动白名单""启动黑名单"没有
对应接口，改用一个命令行驱动器，直接复用产品自身的 `ControllerServer` 类，
调用与 GUI 按钮完全相同的 `push_rules()` / `set_filter()` 方法。

**覆盖范围说明**：该方式覆盖了规则下发、消息协议、被控端全部处理逻辑与
网络操作；未覆盖 GUI 按钮点击到处理函数之间的界面接线。后者逻辑简单、风险低。

---

## 四、功能验证：九项通过

| # | 功能 | 判据 | 结果 |
|---|------|------|------|
| 1 | 服务安装 | 启动类型 Auto、LocalSystem 身份 | 通过 |
| 2 | 开机默认断网 | 启动即删默认路由，normal → disconnect | 通过 |
| 3 | 注册与权威状态下发 | 按教师机记录的状态放行，不听被控端自报 | 通过 |
| 4 | 状态持久化 | 教师机重启后仍能恢复各机上次状态 | 通过 |
| 5 | 白名单模式 | 白名单站点 200，其余 NXDOMAIN | 通过 |
| 6 | 白名单动态加路由 | CDN 的 IP 随 DNS 查询实时进路由表 | 通过 |
| 7 | 黑名单模式 | 黑名单站点 NXDOMAIN，其余正常 | 通过 |
| 8 | 禁止上网 | 默认路由消失，但仍能连教师机 | 通过 |
| 9 | 状态切换清理 | `/32` 路由由 12 条降回 2 条，无残留 | 通过 |

### 4.1 开机默认断网

**这个功能解决什么问题**：机房机器常装还原卡，每天重启后系统还原。学生开机后，
从系统启动到教师机把它管起来之间有一两分钟空当，这段时间不受管控。

fail-closed 的做法是：**学生机一启动就先自行断网，再连教师机，由教师机下发
权威状态。**

这里有个鸡生蛋问题：断网了怎么连教师机？代码的解法是在删除默认路由**之前**，
先给教师机的 IP 单独加一条主机路由——通往互联网的路断了，通往教师机的路留着。

完整重启后的被控端日志：

```
18:31:49 [agent]    被控端启动
18:32:00 [agent]    开机默认断网（fail-closed），等待主控端下发上次状态
18:32:07 [firewall] 网卡 DNS 已设置为 114.114.114.114
18:32:17 [firewall] 已清除所有 NC_ 防火墙规则，默认出站已恢复
18:32:21 [firewall] 本机直连网段: ['192.168.31.0/24']
18:32:26 [firewall] 跳过 1 个同网段 IP（on-link 直达，无需主机路由）: ['192.168.31.1']
18:32:31 [firewall] 已删除默认路由（共 1 条），互联网已断开，局域网保留
18:32:31 [agent]    状态: normal → disconnect
18:32:31 [ws_client] 连接主控端: ws://192.168.31.1:8765
18:32:32 [ws_client] 已连接主控端              ← 用时 156 毫秒
18:33:03 [firewall] 已恢复默认路由: 192.168.31.2 (IF 11)
18:33:08 [agent]    状态: disconnect → normal
```

同一时刻教师机日志：

```
18:32:07 新连接: 192.168.31.132
18:32:07 注册: 192.168.31.132
18:32:07 [重连恢复] 192.168.31.132 恢复断开前状态: normal
18:32:43 ACK: 已恢复上网
```

闭环走通：开机 → 自行断网 → 连教师机 → 教师机下发权威状态 → 恢复网络。

**这是修复之后的结果。**修复前日志停在"连接主控端"一行，之后每 10 秒超时、
每 5 秒重试，永远连不上（见[问题一](#问题一同网段教师机被保命路由挡在门外)）。

### 4.2 白名单的实现原理

本系统的白名单不依赖防火墙，而依赖**路由表**。

打个比方：防火墙相当于在门口安排保安查证件；本系统的做法是**直接把通往外部的
路挖断，只给白名单站点单独修一条小路**。

1. 删除默认路由 → 除局域网外无法访问任何地方
2. 网卡 DNS 改为 `127.0.0.1`，所有域名查询经过本机过滤服务
3. 访问白名单站点时，本地 DNS 向上游查到真实 IP 后，**当场为该 IP 添加主机路由**，
   然后才把结果返回给浏览器
4. 访问非白名单站点，本地 DNS 直接返回 NXDOMAIN

**第 3 步为何要实时添加而非提前解析**：大站点普遍使用 CDN，同一域名每次查询
返回的 IP 可能不同，提前解析的结果第二次就失效了。

**相比防火墙方案的优势**：即使绕过 DNS 直接用 IP 访问其他站点也无效，
因为该 IP 没有路由，数据包发不出去。

**动态加路由的实测证据**：连续采集两次，主机路由由 4 条增至 12 条，
新增的 6 条全部是白名单站点的 CDN 地址。且两次查询返回的 IP 段完全不同
（`36.163.75.x` → `36.156.33.x`），印证了 CDN 轮换问题的存在。

### 4.3 各状态的实测数据

**正常状态**

```
默认路由        = 有 (1 条) -> 192.168.31.2
/32 主机路由    = 2 条
网卡 DNS       = 114.114.114.114
本地 DNS 53    = 未监听
NC_ 规则       = 0 条
三个站点访问    = 200 / 200 / 200
```

**白名单状态**

```
默认路由        = 没有（互联网已切断）
/32 主机路由    = 3 条 → 12 条（随 DNS 查询增长）
网卡 DNS       = 127.0.0.1
本地 DNS 53    = 监听中
到教师机 TCP    = 通
白名单站点      = 解析成功，HTTP 200
非白名单站点    = NXDOMAIN，无法访问
```

**黑名单状态**

```
默认路由        = 有 (1 条) -> 192.168.31.2
/32 主机路由    = 12 条 → 2 条（白名单路由已清理干净）
网卡 DNS       = 127.0.0.1
黑名单站点      = NXDOMAIN
其余站点        = 正常 200
```

**断网状态**

```
默认路由        = 没有
网卡 DNS       = 114.114.114.114（恢复为上游，不再劫持）
本地 DNS 53    = 未监听
到教师机 TCP    = 通          ← 关键：这是能被放行的前提
三个站点访问    = 全部失败
```

---

## 五、发现并修复的缺陷

### 问题一：同网段教师机被"保命路由"挡在门外

**发现方式**：真机测试　**严重程度**：严重　**状态**：已修复并复验

#### 现象

学生机重启后进入断网状态，随后连教师机失败：

```
18:18:29 连接主控端: ws://192.168.31.1:8765
18:18:39 WARNING: 连接断开:            ← 10 秒超时
18:18:39 5s 后重连...
18:18:44 连接主控端: ws://192.168.31.1:8765
18:18:54 WARNING: 连接断开:            ← 又超时
```

每 5 秒重试一次，永远失败。该机器就此失联——教师机上看不到它，
也无法将其放开，**因为放开指令要经网络下发，而网络已断**。

#### 成因

两台机器通信有两种情况：

- **同一网段**（如均为 `192.168.31.x`）：直接通信，无需经过路由器
- **不同网段**：必须交由路由器转发

断网前，为保住通往教师机的路，代码给教师机 IP 添加了一条主机路由，
并**指定下一跳为默认网关**。

问题在于：教师机 `192.168.31.1` 与学生机 `192.168.31.132` **本就同网段**，
直接通信即可，无需经过网关。

而 Windows 的路由选择规则是"越具体的路由优先"。这条针对单个地址的 `/32` 路由，
比"整个网段直连可达"的 `/24` 路由更具体，**因此胜出**。

结果是：本可直接送达的数据包被强制交给网关；而网关收到目标就在来源网段内的
数据包时，许多实现会直接丢弃。

#### 对照实验

```
【阶段一】当前路由情况
  主机路由 : 192.168.31.1/32   → 下一跳 192.168.31.2   metric 1
  网段路由 : 192.168.31.0/24   → 直连可达              metric 256
  默认路由 : 无（已被断网逻辑删除）
  实际生效 : 192.168.31.1/32 经 192.168.31.2       ← 主机路由胜出

【阶段二】带该主机路由测连通性
  TCP 192.168.31.1:8765  = 失败
  PING 192.168.31.1      = 失败

【阶段三】删除该主机路由后重测
  实际生效 : 192.168.31.0/24 直连可达
  TCP 192.168.31.1:8765  = 成功
  PING 192.168.31.1      = 成功

【结论】确认：该主机路由即为连不上教师机的直接原因
```

#### 影响范围

- **NAT 环境**：确定失败，已实测
- **真实机房**：取决于网关行为。多数商用路由器会做 hairpin 转发并回 ICMP
  重定向，此时能连上、只是多绕一跳
- **失败场景**：配置了 `no ip redirects` 的三层交换机、禁用 hairpin 的设备、
  做了 VLAN 隔离的网络

机房中教师机与学生机**几乎总是同网段**，因此这条有缺陷的代码路径在真实部署中
必然被触发，能否工作取决于网关实现。

#### 修复

增加直连网段判断：**目标 IP 若与本机任一网卡同网段，则不添加主机路由。**

理由是同网段本就直连可达，且删除默认路由的操作不影响同网段通信，
该路由始终存在，无需额外保留。

- 新增 `_refresh_onlink_nets()`：查询本机所有网卡的 IP 与前缀长度，计算直连网段
- 新增 `_is_onlink(ip)`：判断 IP 是否落在直连网段内
- `_add_host_routes_bulk()` 过滤同网段 IP，但仍将其计入"已处理"返回，
  避免调用方反复重试
- 断网与白名单两条路径在添加路由前各刷新一次网段信息

#### 复验

修复后重新打包并完整重启虚拟机，连接在 **156 毫秒**内建立成功
（修复前为每次 10 秒超时、无限重试）。

---

### 问题二：教师机上报了错误的自身 IP

**发现方式**：真机测试　**严重程度**：严重　**状态**：已修复并复验

#### 现象

被控端日志：

```
18:40:49 [firewall] 断网保留主控端路由: 172.16.5.20 经 192.168.31.2
```

教师机实际地址是 `192.168.31.1`，却下发了 `172.16.5.20`。

#### 成因

教师机通过以下方式探测自身 IP：

```python
s.connect(("8.8.8.8", 80))
ip = s.getsockname()[0]
```

这个技巧回答的是**"出站上网走哪张网卡"**，而不是**"被控端从哪张网卡连进来"**。

教师机有两张网卡：一张通往互联网，一张面向学生机所在网段。
探测结果是前者，但学生机实际是从后者连入的。

#### 影响

教师机只要有第二张网卡——有线加无线、VPN、虚拟机软件、USB 网卡——
下发的地址就可能是错的。

若教师机恰好**不在**学生机网段（此时问题一的同网段判断无法覆盖），
学生机断网时会为错误地址添加路由，同样连不回教师机。

两个问题叠加后可以看出：修复之前，能正常工作的只有
"教师机不同网段 **且** 教师机单网卡"这一种组合。

#### 修复

不再全局探测，改为**取每条连接的本端地址**——被控端连到哪个 IP，就下发哪个 IP。

- `ConnectedAgent` 新增 `local_ip` 字段，连接建立时记录 `ws.local_address[0]`
- `push_rules()` 由"生成一条消息广播"改为"逐台生成消息"
- 重连时刷新该字段（可能落在另一张网卡上）

#### 复验

修复后日志显示下发的是正确的 `192.168.31.1`，且错误地址的主机路由不再出现在
路由表中。

---

### 问题三：卸载竞态导致网络恢复被中断

**发现方式**：代码审查　**严重程度**：严重　**状态**：已修复并复验

#### 成因（三层叠加）

**第一层：`CTR.exe stop` 不等待。**
pywin32 源码确认，不带 `--wait` 时调用的 API 语义是发出停止信号后立即返回，
不等待服务实际停止。

**第二层：服务停止时的清理很慢。**
服务收到停止信号后执行 `_do_restore()` 恢复网络，需要 8 次 PowerShell 调用。
实测耗时：

```
空命令（纯 PowerShell 启动开销）        0.86 秒
Get-NetRoute 查默认路由                 1.28 秒
Get-NetAdapter 数网卡                   1.40 秒
Get-NetFirewallRule 过滤防火墙规则       2.34 秒
                                       ────────
8 次调用合计                       约 10 ~ 12 秒   vs   脚本等待 2 秒
```

PowerShell 仅启动就接近 1 秒，这是耗时的主要来源。

**第三层：恢复顺序把最关键的放在最后。**

```
停 DNS → 改回网卡 DNS → 清防火墙 → 恢复默认路由
                  ↑
            2 秒大致到这里
```

被终止在第二步的结果是：DNS 恢复了，**默认路由没恢复**。

#### 后果

若卸载前学生机处于断网或白名单状态，卸载后该机器**永久无法上网**。
网关备份文件仍在原目录，但读取它的程序已被卸载，只能手工执行 `route add`
或重装网卡驱动。

#### 修复

```
CTR.exe --wait 40 stop
```

**注意 `--wait` 必须写在 `stop` 之前。** pywin32 使用 getopt 解析参数，
遇到第一个非选项参数即停止解析：

```
CTR.exe --wait 40 stop   →  waitSecs=40   轮询等待服务真正停止   ✓
CTR.exe stop --wait 40   →  waitSecs=0    退化为发信号立即返回   ✗（静默失效）
```

#### 复验

在虚拟机上先将学生机置于**断网状态**，再执行卸载：

```
卸载前：默认路由 = 无，三个站点全部无法解析
卸载后：默认路由 = 有 → 192.168.31.2，三个站点全部 200
```

实测该命令耗时 56 秒，确认它在等待服务完成清理。

---

### 问题四：路由清理超命令行长度上限

**发现方式**：代码审查　**严重程度**：严重　**状态**：已修复

#### 成因

白名单模式累积的 CDN 地址在清理时被拼接为**一条** PowerShell 命令。
每个地址约占 119 字符，约 275 个地址即超过 Windows `CreateProcessW` 的
32767 字符上限。

超限不是截断，而是**直接抛出异常**：

```
命令长度 30013 字符  →  正常执行
命令长度 33013 字符  →  FileNotFoundError: [WinError 206] 文件名或扩展名太长
```

#### 后果比崩溃更糟

执行命令的函数当时没有异常兜底，异常会向上穿透到状态机。
而状态机在函数开头就已更新状态与托盘图标，真正的网络操作在其后才抛异常，
于是：

- 教师机界面显示**正常**，托盘图标显示**正常上网**
- 学生机实际**默认路由未恢复，仍处于断网**
- 心跳持续汇报"正常状态"，教师机无从察觉
- 重复操作会重复触发同样的异常，**无法自愈**

唯一线索是被控端日志中一行 `处理消息异常: [WinError 206] ...`。

#### 数量是否现实

真机实测：**仅两次页面加载，主机路由就从 4 条增至 12 条**。
一节课访问十余个站点，数百条是常态。

#### 修复与验证

- `_run_ps()` 增加异常兜底，返回 `(False, 错误信息)` 而非抛出
- 新增 `_run_ps_batched()`，按命令行长度上限自动分批（上限设为 28000 字符）

模拟 600 个 CDN 地址的验证结果：

```
修复前（不分批）
   单条命令 69281 字符，超限 36514 → 抛 WinError 206

修复后
   清理路径：3 次调用，各批 [27936, 27948, 13395] 字符
            最长 27948 < 32767，残留路由 0 条
   添加路径：8 次调用，输出解析回 600/600 个 IP，无丢失
   执行函数：超长命令返回 ok=False，不再抛异常穿透状态机
```

---

### 问题五：HTTP 控制接口可被 CSRF

**发现方式**：代码审查　**严重程度**：严重　**状态**：已修复并复验

#### 成因

控制接口有三个设计叠加：默认不校验令牌、GET 请求即可变更状态、
对所有来源开放（`Access-Control-Allow-Origin: *`）。

#### 攻击方式

教师机浏览器打开任意网页，网页中包含：

```html
<img src="http://127.0.0.1:8770/api/network/disable" style="display:none">
```

浏览器会照常发出该请求，导致全部被控端断网，操作者无感知。

**绑定 `127.0.0.1` 无法防护**：请求正是从教师机自身的浏览器发出的。

**同源策略无法防护**：同源策略限制的是"读取响应"，而非"发出请求"；
此攻击不需要读取响应，副作用在服务端已经产生。

#### 修复

基于 **Fetch Metadata** 识别浏览器发起的跨站请求：

| 调用方式 | `Sec-Fetch-Site` | 处理 |
|---------|-----------------|------|
| curl / PowerShell / Python / 外部程序 | 不发送 | 放行 202 |
| 浏览器地址栏直接访问 | `none` | 放行 202 |
| 同源页面的 fetch | `same-origin` | 放行 202 |
| 恶意页面的 `<img>` | `cross-site` | 拦截 403 |
| 恶意页面的 `<form>` / fetch / 跳转 | `cross-site` | 拦截 403 |

**该方案不破坏任何既有用法**：文档中记载的浏览器地址栏访问与 curl 调用
均照常可用，仅拦截恶意页面嵌入的请求。

同时移除 `Access-Control-Allow-Origin: *`，防止任意网页跨源读取
`/api/status`（其中包含全部被控端的 IP、主机名与最近访问域名）。

若令牌通过**请求头**（`Authorization` / `X-API-Token`）提供，则跳过此检查——
能设置自定义请求头即说明不是 `<img>` 型 CSRF。`?token=` 拼在 URL 中不算，
因为可被伪造。

#### 复验

在正式打包的 exe 上执行 11 个用例，全部符合预期。教师机日志：

```
[api_server] WARNING: 已拦截疑似 CSRF 请求 GET /api/network/disable
                      （Sec-Fetch-Site: cross-site）
[api_server] INFO: "GET /api/network/disable HTTP/1.1" 403 -
[api_server] WARNING: 已拦截疑似 CSRF 请求 GET /api/network/disable
                      （跨源 Origin: http://evil.example.com）
[api_server] INFO: "GET /api/network/disable HTTP/1.1" 403 -
```

**局限**：不发送 Fetch Metadata 请求头的老旧浏览器可绕过此检查。
需彻底防护请设置 `api_token`。

---

### 问题六：依赖清单缺两个包

**发现方式**：代码审查　**严重程度**：中　**状态**：已修复

`requirements.txt` 原有 6 个包，但代码还使用了 `requests` 与 `beautifulsoup4`。
调用链全为模块级导入，无延迟加载：

```
controller/main.py           →  MainWindow
  main_window.py             →  RulePanel
    rule_panel.py 第 16 行    →  dep_discover
      dep_discover.py         →  import requests
                              →  from bs4 import BeautifulSoup
```

三条路径中只有一条是断的：开发环境曾手动安装过，打包配置的 hiddenimports
显式列出了这两个包，**唯独按文档执行 `pip install -r requirements.txt`
再运行会直接 ImportError**。

修复后通过 AST 扫描全部 18 个源文件的 import 语句，剔除标准库与本地模块后
共 19 个第三方顶层模块（含 10 个 pywin32 子模块），确认已全部被依赖清单覆盖。

---

### 问题七：卸载脚本与自动重启策略冲突

**发现方式**：真机测试　**严重程度**：中　**状态**：已修复

安装时配置了服务失败自动重启策略（崩溃后 30 秒重启），用于防止服务被随意终止。

但卸载脚本中的 `taskkill /f` 在服务控制管理器看来等同于服务崩溃，
于是 30 秒后服务被重新拉起，后续的 `remove` 无法真正删除服务注册。
真机验证显示卸载流程结束后服务仍处于 Running 状态。

**修复**：卸载前先禁用自启并解除失败重启策略，再执行停止、终止、移除。
同样的防护加入安装脚本的升级路径。

---

## 六、已确认但未修复的问题

### 6.1 黑名单模式可被绕过

**严重程度**：中（功能名不副实）

黑名单模式仅做 DNS 拦截，且执行时会清空防火墙规则（实测 `NC_` 规则为 0 条），
没有任何机制阻止使用其他方式解析域名。

实测结果：

```
A) 使用系统 DNS（已劫持到 127.0.0.1）
      BLOCKED（NXDOMAIN）                      ← 黑名单生效

B) 指定 8.8.8.8 查询
      解析成功，到该 IP 的 TCP 443 = 通         ← 绕过

B) 指定 223.5.5.5 查询
      解析成功，到该 IP 的 TCP 443 = 通         ← 绕过

B) 指定 114.114.114.114 查询
      解析成功，到该 IP 的 TCP 443 = 通         ← 绕过
```

不仅能解析，**解析出的 IP 443 端口实测可达**，即目标站点确实可以访问。
一条指定 DNS 服务器的查询命令即可绕过，无需修改网卡设置。

更进一步，主流浏览器默认开启的 DNS over HTTPS 走 443 端口加密连接，
完全绕过系统 DNS。

**对比**：白名单模式基于"删除默认路由 + 只为放行 IP 添加路由"，不依赖 DNS，
即使用其他 DNS 查到 IP 也无路由可达。实测确认白名单模式下非白名单站点彻底不通。

**两种处理方向**：

1. 补充出站 53 / 853 端口封锁与已知 DoH 服务商 IP 封锁，使黑名单成为真管控。
   工作量约 1 小时，但 DoH 服务器 IP 会变化，属于持续对抗。
2. 在文档中明确黑名单为软提醒，需要强管控时使用白名单模式。

考虑到系统已具备一个无法绕过的白名单模式，方向二更为务实——
把黑名单描述为强管控反而会造成安全错觉。

### 6.2 服务停止后进程不退出

**严重程度**：低（有兜底，不影响功能）

服务控制管理器已报告 STOPPED，但进程仍以 SYSTEM 身份存活。

原因是主循环中的网络监控任务是 `while True` 无限循环，永不退出，
导致 `asyncio.gather()` 永不完成、服务入口函数无法返回、进程不退出。

当前由卸载脚本的 `taskkill /f /t` 兜底。彻底的做法是为该监控任务增加停止事件，
使主循环能够正常收敛。

---

## 七、部署与测试经验

| 事项 | 说明 |
|------|------|
| **空密码账户无法用 vmrun 操作** | Windows 安全策略（`LimitBlankPasswordUse`）规定空密码账户只允许本地控制台登录，不接受服务式认证。需为账户设置密码。<br>不需要凭据的操作：开关机、快照、回滚、挂载共享文件夹；需要凭据的：传文件、执行命令、截屏。 |
| **中文 PowerShell 脚本可能解析失败** | 不带 BOM 的 UTF-8 文件会被 PowerShell 5.1 按 ANSI 解读，中文字节被错误解码后可能破坏引号配对，导致脚本解析失败且无有效报错。投送执行的脚本建议使用纯 ASCII 或添加 BOM。 |
| **`timeout` 在无控制台环境会挂死** | 脚本结尾常见的 `timeout /t N` 在没有交互式控制台时会无限等待。**批量部署使用远程执行时必然触发**。已改为 `ping -n N 127.0.0.1 >nul`。 |
| **PyInstaller 单文件 exe 会锁住自身** | 服务停止后进程仍存活会导致 exe 被占用，替换时报无访问权限。升级流程需先确认进程已退出。 |
| **VMware Tools 提供管理员权限** | Tools 以 SYSTEM 身份运行，经它启动的程序天然具备管理员权限，不弹 UAC，可实现全自动安装服务。 |

---

## 附录：名词解释

| 名词 | 说明 |
|------|------|
| **默认路由** | "无法匹配其他路由的数据包送往此处"。删除后除本地网段外无法访问任何地址，即断网 |
| **`/32` 主机路由** | 针对单个 IP 地址的路由。`/32` 表示精确到该地址，是最具体的路由形式 |
| **on-link（直连）** | 目标与本机处于同一网段，可直接通信，无需经过路由器 |
| **NXDOMAIN** | DNS 应答码，表示"该域名不存在"。本地 DNS 服务用它来拦截站点 |
| **fail-closed** | 失败时关闭。系统异常或状态未知时默认选择最安全的行为（断网）而非放行 |
| **CDN** | 内容分发网络。同一域名每次查询可能返回不同 IP |
| **DoH** | DNS over HTTPS。DNS 查询经 HTTPS 走 443 端口，绕过传统 DNS 拦截 |
| **CSRF** | 跨站请求伪造。恶意网页借用浏览器的身份向其他服务发起请求 |
| **SCM** | Windows 服务控制管理器，负责服务的启动、停止与监控 |
| **hairpin** | 发夹弯转发。路由器将数据包从进入的接口原路发回，用于同网段设备经路由器互访 |

---

*验证环境：VMware Workstation · Windows 10 Pro 19045 客户机*
